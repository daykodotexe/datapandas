[project]
name = "datapandas"
version = "0.2.0"
description = "Lightweight data quality and profiling tools for pandas DataFrames"
dependencies = ["pandas"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
